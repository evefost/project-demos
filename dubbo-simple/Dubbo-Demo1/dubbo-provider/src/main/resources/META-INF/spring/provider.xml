<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/beans        http://www.springframework.org/schema/beans/spring-beans.xsd        http://code.alibabatech.com/schema/dubbo        http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <!-- 提供方应用信息，用于计算依赖关系 -->
    <dubbo:application name="hello-world-provider" owner="parker"/>

    <dubbo:monitor protocol="registry"></dubbo:monitor>

    <dubbo:registry address="zookeeper://101.200.129.112:2181" id="zk"/>

    <dubbo:protocol name="dubbo" port="20880"/> <!--dubbo协议-->

    <!-- 声明需要暴露的服务接口 -->
    <dubbo:service interface="com.dn.dubbo.demo.api.IHelloWorldService" ref="demoService" protocol="dubbo"
                   registry="zk"></dubbo:service>

    <!-- 和本地bean一样实现服务 -->
    <bean id="demoService" class="com.dn.dubbo.demo.provider.HelloWorldServiceImpl"/>

</beans>